name: Hello World
on:
    workflow_dispatch:
        inputs:
            environment:
                description: 'Environment'
                type: environment
                required: true
jobs:
    hello-job:
        runs-on: ubuntu-latest
        steps:
        - name: Echo text
          run: echo "Hello, world!"